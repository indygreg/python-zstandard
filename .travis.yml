language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.6-dev"
  - "3.7-dev"
  - "nightly"

matrix:
  include:
    # Add builds for Conda.
    - python: 2.7
      env: CONDA=1
      env: PIP=pip
      sudo: required
      services:
        - docker
    - python: 3.5
      env: CONDA=1
    - python: 3.6
      env: CONDA=1
    - os: osx
      language: generic
      env: PIP=pip2

env:
  global:
    - HYPOTHESIS_PROFILE=ci

install:
  - source ci/travis-install.sh

script:
  - source ci/travis-build.sh
